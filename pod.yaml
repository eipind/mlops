apiVersion: v1
kind: Pod
metadata:
  name: flask-pod
  labels:
    app: flask
spec:
  containers:
  - name: flask-app
    image: mlops_task
    imagePullPolicy: Never
    ports:
    - containerPort: 5000
